
server:
  port: 8888
spring:
  application:
    name: configserver
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Juanjosan96/Bootcamp.git
        default-label: main
        search-paths:
        - '*service*'